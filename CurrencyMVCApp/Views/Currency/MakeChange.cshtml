@model CurrencyMVCApp.ViewModels.RepoViewModel

@{
    ViewData["Title"] = "MakeChange";
}

<h1>MakeChange</h1>
<hr />
<div>
    <form asp-action='MakeChange' class='d-flex align-items-center'>
        <div class="form-group">
            <label htmlfor="Amount" class="control-label"></label>
            <input name="Amount"  type="number" step="0.01" class="form-control" />
        </div>
        <div class="form-group">
            <input type="submit" value="Save" class="btn btn-primary" />
        </div>
    </form>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.TotalValue)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.TotalValue)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}